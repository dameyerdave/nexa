############
# ALL stack configuration lives here.
#
# 1. Copy this file to .env
# 2. Generate secrets:  sh scripts/generate-keys.sh --update-env
# 3. Fill in the Google OAuth client (see README.md "Google SSO setup")
# 4. docker compose up -d
#
# YOU MUST CHANGE ALL THE DEFAULT SECRET VALUES BELOW BEFORE STARTING
# THE CONTAINERS FOR THE FIRST TIME.
############


############
# Portal - the minimalistic landing page users see first
############

# Shown in the portal UI and as the Metabase site name. Keep this generic -
# it is deliberately what end users see instead of "Supabase" / "Metabase".
APP_NAME=Nexdata

# Port the portal is published on.
PORTAL_PORT=3100

DATA_MODEL_LABEL=Data Model
DATA_ANALYTICS_LABEL=Data Analytics

# Comma-separated allowlist of emails treated as portal admins. Admins can
# open /admin/users to grant other users roles:
#   dbadmin         - automatic (no extra login) access to the Data Model
#                     (Supabase Studio) tile
#   dashboardadmin  - edit access in Metabase (everyone else is read-only,
#                     see scripts/setup_metabase_permissions.py)
# Leave empty and no one can manage roles; set it to your own email before
# first deploy.
PORTAL_ADMIN_EMAILS=


############
# Postgres
############

POSTGRES_HOST=db
POSTGRES_DB=postgres
# Default configuration includes Supavisor exposing POSTGRES_PORT
POSTGRES_PORT=5432
POSTGRES_PASSWORD=your-super-secret-and-long-postgres-password


############
# Secrets - generate with: sh scripts/generate-keys.sh --update-env
############

# Legacy symmetric HS256 key used to sign/verify all Supabase JWTs.
JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters-long
JWT_EXPIRY=3600

# Legacy API keys (HS256-signed JWTs), derived from JWT_SECRET.
ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyAgCiAgICAicm9sZSI6ICJhbm9uIiwKICAgICJpc3MiOiAic3VwYWJhc2UtZGVtbyIsCiAgICAiaWF0IjogMTY0MTc2OTIwMCwKICAgICJleHAiOiAxNzk5NTM1NjAwCn0.dc_X5iR_VP_qT0zsiyj_I_OZ2T9FtRU2BBNWN8Bu4GE
SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyAgCiAgICAicm9sZSI6ICJzZXJ2aWNlX3JvbGUiLAogICAgImlzcyI6ICJzdXBhYmFzZS1kZW1vIiwKICAgICJpYXQiOiAxNjQxNzY5MjAwLAogICAgImV4cCI6IDE3OTk1MzU2MDAKfQ.DaYlNEoUrrEn2Ig7tqibS-PHK5vgusbcbo7X36XVt4Q

# Opaque API keys (new-style). Optional - leave blank to use the legacy keys
# above only.
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
JWT_KEYS=
JWT_JWKS=
ANON_KEY_ASYMMETRIC=
SERVICE_ROLE_KEY_ASYMMETRIC=

# HTTP Basic Auth in front of the Supabase Studio dashboard (Kong "/" route).
# Studio has no built-in SSO of its own for self-hosted deployments, so it
# stays behind these admin credentials in addition to the portal login.
DASHBOARD_USERNAME=supabase
DASHBOARD_PASSWORD=this_password_is_insecure_and_should_be_updated

# (Must be at least 64 characters; generate with: openssl rand -base64 48)
SECRET_KEY_BASE=UpNVntn3cDxHJpq99YMc1T1AQgQpc8kfYTuRgBiYa15BLrx8etQoXz3gZv1/u2oq
# (Must be exactly 16 characters; generate with: openssl rand -hex 8)
REALTIME_DB_ENC_KEY=supabaserealtime
# (Must be exactly 32 characters; generate with: openssl rand -hex 16)
VAULT_ENC_KEY=your-32-character-encryption-key
# (Must be at least 32 characters; generate with: openssl rand -base64 24)
PG_META_CRYPTO_KEY=your-encryption-key-32-chars-min

# S3-protocol endpoint credentials for Storage.
S3_PROTOCOL_ACCESS_KEY_ID=625729a08b95bf1b7ff351a663f3a23c
S3_PROTOCOL_ACCESS_KEY_SECRET=850181e4652dd023b7a98c58ae0d2d34bd487ee0cc3254aed6eda37307425907


############
# URLs - update these to your real domain(s) in production
############

# Public URL for the Supabase API / Studio, i.e. what the portal links to
# for "Data Model" access.
SUPABASE_PUBLIC_URL=http://localhost:8000
API_EXTERNAL_URL=http://localhost:8000/auth/v1
SITE_URL=http://localhost:3100
ADDITIONAL_REDIRECT_URLS=http://localhost:3100/auth/callback

DISABLE_SIGNUP=false


############
# Kong - API gateway ports
############

KONG_HTTP_PORT=8000
KONG_HTTPS_PORT=8443


############
# Supavisor - Postgres connection pooler
############

POOLER_PROXY_PORT_TRANSACTION=6543
POOLER_DEFAULT_POOL_SIZE=20
POOLER_MAX_CLIENT_CONN=100
POOLER_TENANT_ID=your-tenant-id
POOLER_DB_POOL_SIZE=5


############
# Studio
############

STUDIO_DEFAULT_ORGANIZATION=Default Organization
STUDIO_DEFAULT_PROJECT=Default Project
# Optional, enables the Studio AI Assistant.
OPENAI_API_KEY=


############
# Auth (GoTrue) - Mailer / email sign-up
############

MAILER_URLPATHS_CONFIRMATION="/auth/v1/verify"
MAILER_URLPATHS_INVITE="/auth/v1/verify"
MAILER_URLPATHS_RECOVERY="/auth/v1/verify"
MAILER_URLPATHS_EMAIL_CHANGE="/auth/v1/verify"

ENABLE_EMAIL_SIGNUP=true
# Auto-confirms new accounts so email/password sign-up works out of the box
# without SMTP configured. Set to false once real SMTP is in place below,
# so new accounts must confirm their address via email.
ENABLE_EMAIL_AUTOCONFIRM=true
SMTP_ADMIN_EMAIL=admin@example.com
SMTP_HOST=
SMTP_PORT=2500
SMTP_USER=
SMTP_PASS=
SMTP_SENDER_NAME=Nexdata
ENABLE_ANONYMOUS_USERS=false

ENABLE_PHONE_SIGNUP=false
ENABLE_PHONE_AUTOCONFIRM=false


############
# Auth (GoTrue) - OAuth / SSO providers
#
# The portal currently signs users in with email + password (works out of
# the box, no external setup required). Supabase Auth also supports many
# external identity providers (Google, GitHub, Azure/Microsoft Entra ID,
# GitLab, Bitbucket, Discord, Facebook, X/Twitter, Apple, Slack, Spotify,
# LinkedIn, Notion, WorkOS, Zoom, and enterprise SSO via SAML) - see
# https://supabase.com/docs/guides/auth/social-login and
# https://supabase.com/docs/guides/auth/enterprise-sso/auth-sso-saml
#
# Google is wired up below as the reference implementation (set
# GOOGLE_ENABLED=true and fill in the credentials to turn it on) and is
# also reused by Metabase's native Google Sign-In (see MB_* section), so
# signing in once with Google gives access to both the Data Model and Data
# Analytics destinations. To add another provider: add its matching
# GOTRUE_EXTERNAL_<PROVIDER>_* variables in docker-compose.yml (a couple of
# examples are stubbed out there already) and fill in its credentials here.
############

GOOGLE_ENABLED=false
# OAuth 2.0 Client ID / Secret from Google Cloud Console
# (APIs & Services > Credentials > OAuth client ID > Web application).
# Authorized redirect URI: ${API_EXTERNAL_URL}/callback
#   e.g. http://localhost:8000/auth/v1/callback
GOOGLE_CLIENT_ID=
GOOGLE_SECRET=

# Optional: restrict Metabase Google sign-in/auto-provisioning to a domain
# (e.g. "example.com"). Leave blank to allow any Google account that has
# already been invited/created in Metabase.
GOOGLE_AUTH_ALLOWED_DOMAIN=

# GITHUB_ENABLED=false
# GITHUB_CLIENT_ID=
# GITHUB_SECRET=

# AZURE_ENABLED=false
# AZURE_CLIENT_ID=
# AZURE_SECRET=


############
# Storage
############

GLOBAL_S3_BUCKET=stub
REGION=stub
STORAGE_TENANT_ID=stub


############
# Edge Functions
############

FUNCTIONS_VERIFY_JWT=false


############
# PostgREST
############

# Postgres schemas exposed via the REST API and visible in Studio's table
# editor. Add an app's schema_namespace here (e.g. `biomed` for
# apps/biomedical-studies) only after applying it - see apps/README.md -
# a schema listed here that doesn't exist yet breaks PostgREST's startup.
PGRST_DB_SCHEMAS=public,graphql_public
PGRST_DB_MAX_ROWS=1000
PGRST_DB_EXTRA_SEARCH_PATH=public


############
# imgproxy
############

IMGPROXY_AUTO_WEBP=true


############
# Metabase ("Data Analytics")
############

METABASE_PORT=3200
METABASE_PUBLIC_URL=http://localhost:3200

# Metabase's own application data lives in a `metabase` database on the
# same Supabase Postgres instance (see volumes/db/metabase.sql) - no
# separate database or credentials to configure here.

# Used once by scripts/apply_dashboards.py to complete Metabase's first-run
# setup wizard via its API (creates this admin account) and to sign in on
# later runs. Safe to leave as-is after first run; rotate the password in
# Metabase itself if you want to change it (this value is not re-applied).
METABASE_ADMIN_EMAIL=admin@example.com
METABASE_ADMIN_PASSWORD=your-super-secret-and-long-metabase-admin-password
METABASE_ADMIN_FIRST_NAME=Admin
METABASE_ADMIN_LAST_NAME=User
