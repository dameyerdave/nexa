# Environment
.env
.env.old

# Local Compose overrides (e.g. swapping in a locally-built image)
docker-compose.override.yml

# Supabase Postgres data / storage (runtime bind mounts)
volumes/db/data
volumes/storage

# Nuxt / Node
portal/node_modules
portal/.nuxt
portal/.output
portal/.nitro
portal/.cache
portal/dist

# Python
__pycache__/
*.pyc

# roles-api (Django) - local SQLite db + collectstatic output
roles-api/data
roles-api/staticfiles
roles-api/db.sqlite3

# OS
.DS_Store
