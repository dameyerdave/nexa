# Environment
.env
.env.old

# Local Compose overrides (e.g. swapping in a locally-built image)
docker-compose.override.yml

# Supabase Postgres data / storage (runtime bind mounts)
volumes/db/data
volumes/storage

# Nuxt / Node
portal/node_modules
portal/.nuxt
portal/.output
portal/.nitro
portal/.cache
portal/dist

# Python
__pycache__/
*.pyc

# OS
.DS_Store
